version: '3'

tasks:
  create:
    dir: "{{ .ZARF_DIR }}"
    cmds:
      - echo {{ .ZARF_DIR }}
      # - zarf package create --confirm

  deploy:
    dir: "{{ .ZARF_DIR }}"
    cmds:
      - zarf package deploy --confirm

  publish:
    dir: "{{ .ZARF_DIR }}"
    cmds:
      - zarf package publish --confirm 
