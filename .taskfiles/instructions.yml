version: '3'

tasks:
  default:
    silent: true
    cmds:
      - cat << EOF
Usage: task [task name(s)] [parameters]

View available tasks:
  task -a

Example:
  task local-deploy
EOF
      - echo 'Usage:'
      - echo '  task [task name]'
      - echo ''
      - echo 'Example:'
      - echo '  task create deploy-tar'
