# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

tasks:
  default:
    silent: true
    cmds:
      - echo 'Usage:'' task [task name(s)] [parameters]'
      - echo ''
      - echo 'View available tasks:'
      - echo '  task -a'
      - echo ''
      - echo 'Example:'
      - echo '  task local-deploy'
